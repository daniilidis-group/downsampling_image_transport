# Downsampling image transport for ROS

This image transport downsamples the frequency at which images are
published. The frequency is dynamically configurable.

Use this transport if you want to drop down the image frequency at
which images are sent.
